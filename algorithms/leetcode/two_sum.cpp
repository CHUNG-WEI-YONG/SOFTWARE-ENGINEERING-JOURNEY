#include <iostream>
#include <vector>
using namespace std;
int max_length=100;
int main(){
    vector<int> two_Sum(vector<int> input, int target){
        vector<int> dict;
        int max_index=input.size();
        for (int i=0;i<max_index;i++){
            int curr=input[i];
            if (curr)
        }
    }

}